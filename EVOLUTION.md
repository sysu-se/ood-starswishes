# Homework 2 · Design Evolution

## 1. 如何实现提示功能？

本次实现把提示能力放在 `Sudoku` 领域对象中，并通过 `Game` 暴露同名接口转发给当前局面。

- `Sudoku#getCandidates(row, col)` 返回某个空格在当前棋盘下可填写的候选数字集合。
- `Sudoku#getNextHint()` 扫描整个棋盘，优先寻找只有一个候选值的格子，作为下一步推定值。
- 如果当前没有唯一候选值，则返回候选集合最小的空格，提示用户可以进入探索。

候选数计算基于数独规则：同一行、同一列、同一宫内已经出现的数字不可再次使用。

## 2. 提示功能更属于 Sudoku 还是 Game？为什么？

提示能力的核心属于 `Sudoku`。原因是候选数和下一步提示只依赖当前棋盘局面与数独约束，不依赖游戏会话状态、历史记录或 UI。

`Game` 仍然提供 `getCandidates` 和 `getNextHint`，但它的职责是把请求转发给当前正在操作的 `Sudoku`。这样 UI 或测试只需要面对 `Game`，而规则判断仍保持在 `Sudoku` 内部。

## 3. 如何实现探索模式？

探索模式由 `Game` 管理，因为它本质上是一次游戏会话状态切换，而不是单纯的棋盘规则。

`Game#enterExplore()` 会记录进入探索前的棋盘快照，之后的 `guess` 会进入探索自己的 undo / redo 栈。探索期间可以：

- `commitExplore()`：提交探索结果，把探索起点作为一次主历史记录，当前探索后的棋盘成为主局面。
- `abandonExplore()`：放弃探索结果，回到探索起点。
- `resetExplore()`：仍留在探索模式中，但把棋盘回滚到探索起点，便于选择另一条路径。

探索失败通过两类方式判断：

1. 当前棋盘出现行、列或宫内重复数字，`hasConflict()` 返回 true。
2. 当前局面曾经被记录为失败探索路径，`isFailedExploration()` 返回 true，`hasConflict()` 也会视为失败。

## 4. 主局面与探索局面的关系是什么？

主局面和探索局面不共享嵌套数组，而是通过深拷贝快照隔离。

进入探索时，`Game` 保存 `origin` 快照。探索中的填写直接作用在当前 `Sudoku` 上，但 `origin` 不会被污染。提交探索时，当前 `Sudoku` 保留为主局面；放弃或重置探索时，用 `origin` 创建新的 `Sudoku` 实例恢复。

这种方案避免了共享二维数组带来的引用污染问题，也让提交和回滚语义比较清楚。

## 5. history 结构在本次作业中是否发生了变化？

主 history 仍然是 Homework 1 中的线性 undo / redo 栈，没有引入复杂 DAG。

变化在于探索模式拥有临时的独立 history：

- 普通模式下，`guess` 写入主 `undoStack`。
- 探索模式下，`guess` 写入探索自己的 `undoStack`。
- 提交探索时，主 history 只记录“从探索起点到探索结果”这一整步变化。
- 放弃探索时，探索 history 被丢弃，主 history 不变化。

这样既保留了线性历史的简单性，又能支持探索过程内部的 undo / redo。

## 6. Homework 1 中哪些设计暴露出了局限？

Homework 1 只需要支持单一路径上的猜测、撤销和重做，因此线性栈已经足够。但 Homework 2 的探索模式需要临时分支和回滚起点，这暴露出两个局限：

1. 单一 history 无法表达“主线”和“探索支线”的区别。
2. 如果 `Sudoku` 直接暴露内部二维数组，探索分支很容易污染主局面。

因此本次实现坚持 `getGrid()` 返回拷贝，并在 `Game` 中区分主 history 与探索 history。

## 7. 如果重做一次 Homework 1，会如何修改原设计？

如果重做 Homework 1，我会从一开始就把 `Sudoku` 和 `Game` 的职责分得更清楚：

- `Sudoku` 只负责棋盘数据、数独规则、候选数、冲突判断、克隆和序列化。
- `Game` 负责会话状态、历史记录、探索状态和失败路径记忆。
- history 的元素统一保存棋盘快照，避免直接保存可变对象引用。

这样 Homework 2 中新增 Hint 和 Explore 时，就不需要推翻原设计，只需要在已有对象职责上自然扩展。
